<p id="notice"><%= notice %></p>

<p>
	<%= debug @photo.image %> 
  <%= image_tag @photo.image.url(:medium) %>
  <%= @photo.description %>
  <%= @photo.profile.name %>
</p>

<% if @photo.profile == current_user.profile %>
	<%= link_to 'Edit', edit_photo_path(@photo) %> |
<% end %>
<%= link_to 'Back', photos_path %>
