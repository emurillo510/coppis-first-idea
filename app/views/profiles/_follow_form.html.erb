<% unless @profile.user == current_user %>
  <div id="follow_form">
  <% if current_user.profile.following?(@profile) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>