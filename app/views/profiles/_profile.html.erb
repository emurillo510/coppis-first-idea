<li>
    <%= link_to image_tag(profile.avatar.url), profile, size: 52 %>
    <%= link_to profile.name, profile%>
    <% if @profile.user == current_user %>
    | <%= link_to "Delete", profile, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>