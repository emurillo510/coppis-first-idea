<%= image_tag @profile.avatar.url(:medium) %>
<h1>
  <%= @profile.name %>
</h1>
<span>
  <li><%= link_to "view my profile", profile_path(current_user, @profile) %></li>
</span>
<span>
  <%= pluralize(@profile.microposts.count, "micropost") %>
</span>